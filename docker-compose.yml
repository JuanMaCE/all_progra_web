version: "3.3"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: django_app:hw06
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    command: sh -c "python3 manage.py migrate && python3 manage.py runserver 0.0.0.0:8000"
